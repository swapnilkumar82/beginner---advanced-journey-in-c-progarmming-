#include <stdio.h>
int main(){

	float x=99.0;
	printf("x=%f\n",x);// but the result is 99.000000 but it a double so, in c both float and double are store in same but before storing it is a float 

	return 0;
}
